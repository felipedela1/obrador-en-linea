name: Daily Stock Reset

on:
  schedule:
    # Ejecutar todos los días a las 6:00 AM UTC (ajusta según tu zona horaria)
    # Para España (UTC+1/+2), esto sería 5:00 AM o 4:00 AM local
    - cron: '0 6 * * *'
  
  # Permitir ejecución manual desde GitHub Actions UI
  workflow_dispatch:

jobs:
  reset-stock:
    runs-on: ubuntu-latest
    
    steps:
      - name: Call Supabase Edge Function
        run: |
          echo "🚀 Ejecutando reset automático de stock diario..."
          
          response=$(curl -s -w "%{http_code}" \
            -X POST \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            "${{ secrets.SUPABASE_URL }}/functions/v1/daily-stock-reset")
          
          http_code="${response: -3}"
          body="${response%???}"
          
          echo "HTTP Status: $http_code"
          echo "Response: $body"
          
          if [ "$http_code" -ne 200 ]; then
            echo "❌ Error: El reset de stock falló con código $http_code"
            echo "$body"
            exit 1
          else
            echo "✅ Reset de stock completado exitosamente"
            echo "$body" | jq '.'
          fi
        
      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ El reset automático de stock ha fallado"
          echo "Revisa los logs y ejecuta el reset manual si es necesario"
